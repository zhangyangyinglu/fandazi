# 饭搭子｜2026-07-12 发布资料清单

## 发布范围

本清单对应本地发布候选提交 `5918cb7`。默认上传整个 Git 仓库，但只包含饭搭子正式工程资料：

- `src/`：React、TypeScript、数据层、页面、状态管理和测试。
- `public/`：209 道菜品引用的 207 张唯一 WebP、食材 WebP、品牌资源、PWA 资源、饭团状态图和 14 张功能图标。
- `supabase/schema.sql`：家庭同步数据库结构，不含账号、密钥或用户数据。
- `docs/`：设计、素材、QA、图标映射和发布审计文档。
- `scripts/`：图片压缩和 GitHub 发布辅助脚本。
- 根目录开源资料：`README.md`、`LICENSE`、`PRIVACY.md`、`DISCLAIMER.md`、`CONTRIBUTING.md`、`CHANGELOG.md`、`.env.example`、CI 与 Vercel 配置。

## 明确排除

以下内容不进入 GitHub：

- `.env.local`、任何 API Key、Supabase service role key、GitHub PAT、Cookie 或账号凭据。
- `node_modules/`、`dist/`、`dev-dist/`、`.vercel/`。
- `backups/`、`dish-image-alpha-workbench/` 等本地备份和视觉 QA 工作区。
- 用户本机 `~/Pictures/`、桌面素材原始路径和未压缩源图。
- 任何家庭真实数据、localStorage 导出、Supabase 生产数据。

## 已完成审计

- 209 个菜品 ID 唯一；209 个图片引用对应 207 个唯一 WebP，0 断链。
- 22 张饭团图标口径已统一：8 张状态图 + 14 张功能图标。
- `.env.local` 已被 `.gitignore` 忽略且未被 Git 跟踪。
- 公开图标 manifest 已移除本机桌面路径。
- 文本源码、文档和配置未发现明文 API Key 模式。
- 本地提交后工作区干净；`git diff --check` 通过。
- 12/12 路由与 PWA 资源、16/16 测试、build、lint 已通过。

## 发布前仍需人工确认

1. 确认图片、图标和品牌素材具备发布/再分发授权。
2. 提供 GitHub PAT，或由用户自行运行 `scripts/publish-github.sh`。
3. 推送后检查 GitHub 仓库首页、README、隐私政策和在线 Demo，再决定是否正式部署。

> 本文件是发布前清单，不代表已经创建 GitHub 远程仓库或完成部署。
