# 07-09 非图标收尾修复记录

## 背景
用户确认：图标部分明天回给 AI，今天继续处理图标以外的剩余问题。

## 本批完成

### 1. MinePage 口味偏好真实化（M7/L3）
- 原问题：`MinePage` 的「口味偏好」Tab 硬编码展示：
  - 咸淡偏好：偏清淡
  - 辣度偏好：微辣
  - 主食偏好：糙米优先
  - 忌口：无
- 修复后：改为读取现有真实数据源 `fantuan.tasteProfile`：
  - `salty` → 咸淡偏好
  - `spicy` → 辣度偏好
  - `sweet` → 甜度偏好
  - `avoid` → 忌口
  - `note` → 备注
- 口味备注/忌口为空时显示明确空状态，不再伪造偏好。

### 2. MinePage 搭子偏好去假数据（M7）
- 原问题：`MinePage` 的「搭子偏好」Tab 硬编码展示“小夏/阿川”的偏好。
- 修复后：由于当前项目内尚无单独的搭子偏好数据源，改为真实空状态：
  - “暂无搭子偏好 / 等待补充”
  - 文案说明：等家庭成员偏好接入后，这里显示每位搭子的忌口、口味和做饭习惯。
- 目的：避免公开 Demo 或真实用户误以为系统已经记录了不存在的搭子偏好。

### 3. 纯图标按钮 aria-label（L4）
- `FamilyPage` 成员删除按钮 `✕` 增加：`aria-label={\`删除成员 ${member.name}\`}`
- 头像 emoji 选择按钮增加：`aria-label={\`将 ${member.name} 的头像设为 ${emoji}\`}`
- 扫描确认：明显的纯图标按钮已无 aria-label 缺失。

## 验证

### 命令验证
- `npx tsc --noEmit`：通过
- `npm run lint`：通过
- `npm run build`：通过

### 浏览器验证
- `/mine` 页面打开正常
- 点击「口味偏好」Tab：展示真实 `fantuan.tasteProfile` 映射结果：
  - 咸淡偏好：适中
  - 辣度偏好：微辣
  - 甜度偏好：甜度适中
  - 忌口：暂无忌口记录
  - 备注：还没有补充口味备注
- 点击「搭子偏好」Tab：展示真实空状态，不再出现“小夏/阿川”假偏好。
- Browser console：0 JS errors。

## 暂停项

### 图标替换
- 用户说明图标明天回给 AI。
- 当前只保留提示词文档：`docs/图标生成提示词清单.md`
- 提示词背景已统一为 `transparent background`，不再要求米白底/暖色背景。

## 下一步
- 等用户明天给图标后：
  1. 统一放入 `public/icons/` 或更细分目录；
  2. 建立 emoji → icon asset 映射；
  3. 替换导航、按钮、分类、饭团形象等所有 emoji；
  4. 跑 tsc/lint/build + 浏览器视觉验证。
