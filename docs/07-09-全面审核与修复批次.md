# 07-09 全面审核与修复批次

## 时间
2026-07-09

## 本批完成

### 第一批：5 个 Bug 修复
1. **Bug1-3 AI Key 家庭共享** — 重写 `aiProviderConfig.ts` 为双层存储（Supabase `household_settings` + localStorage 兜底）；schema 新增表+RLS；`useFamilySync` 初始化拉取云端 Key；PrivacyPage/SyncSettingsPage 文案修正
2. **Bug4 PantryPage 按钮指向** — "添加食材"→"去采购"
3. **Bug5 MinePage 死代码** — 添加 `/mine` 路由 + TopNav 下拉项

### 第二批：8 个严重问题修复
| # | 修复内容 |
|---|---------|
| S1 | 推荐引擎 `recommend.ts` 接入 RecipeWorkspacePage，替换硬编码 `RECOMMENDATION_IDS` |
| S2 | 健康档案 `healthProfiles` 传入 `recommendMeal()`，过敏硬过滤+健康评分生效 |
| S3 | `addCookingLog` 新增连续做菜天数计算（今天/昨天/断裂重置） |
| S4 | Realtime UPDATE 事件先 remove 再 add，避免 pantry/shopping 重复 |
| S5 | 底部导航 display:flex 断点 600px→1000px，覆盖 601-1000px 空白 |
| S6 | RecipeDetailPage "加入计划"新增 added 状态+变绿+"✓ 已加入"反馈 |
| S7 | hero 文案改为 `recommendationResult.reason` 动态生成；侧边栏"快过期"/"缺少食材"从真实数据算 |
| S8 | fantuan 初始值 mili:0/level:1（不再是 128/3 演示数据） |

### 第三批：中等问题修复
| # | 修复内容 |
|---|---------|
| M1 | MinePage 魔术数字 23/12 → 真实值 `cookedDishes.length`/`fantuan.totalCooked` |
| M2 | PlanPage 口味评分 rating 也触发"我家版"沉淀；`MyDishVersion` 类型新增 `rating?` 字段 |
| M5 | PlanPage `pantryNames` 加 `useMemo` |
| M9 | `useFamilySync` 初始化时 `refreshAiConfigFromCloud()` |

### 验证
- tsc 0 错误 / eslint 0 错误 / build 通过
- 浏览器 0 JS 错误
- 首页推荐引擎生效（动态推荐+动态文案+真实快过期食材）
- MinePage 空状态正确（做过 0 道 / Lv.1 / 🌾0）

## 待办（下一轮）

### 中等问题
- M3: Store 死方法 `getPantryMatchForDish`/`getShoppingItemsForPlan`（✅ 当前代码已无此方法）
- M4: ShoppingPage useMemo 依赖 getDishById（✅ 当前 ShoppingPage 已改为直接从 `DISHES` 派生）
- M6: 搜索无清除按钮/空结果提示（✅ 已修：筛选结果提供空状态和清除动作）
- M7: MinePage 口味/搭子偏好 Tab 硬编码（✅ 已修：搭子偏好读取家庭成员资料摘要）

### 轻微问题
- L1: ShoppingPage checkedAuto 集合无限增长（✅ 已修：按当前自动项清理 localStorage）
- L2: 空冰箱+"冰箱可做"筛选无空状态引导（✅ 已修：提供去冰箱添加入口）
- L3: MinePage 口味偏好硬编码（同 M7）
- L4: 部分纯图标按钮缺 aria-label（✅ 已按当前源码审计：浮层关闭/打开/发送、搜索清除、健康删除、成员删除/头像选择、导航更多等图标按钮均有 aria-label；本轮未发现新的无标签纯图标按钮）
- L5: emoji 图标风格不完全统一（✅ 饭团专属入口已统一改用 `FantuanIcon`；食材 emoji、成员头像和饭团状态 emoji 按当前素材边界保留）
- L6: 懒加载 fallback 仅 spinner 无文字（✅ 已修：显示“正在打开饭搭子页面，请稍候…”）

### 图标生成（用户已确认需求）
- 所有 emoji 图标（导航/按钮/分类/饭团形象等）全部替换为自定义生成图标
- 需匹配菜品/食材图风格（干净水彩线稿风）
- 包括饭团形象图
