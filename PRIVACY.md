# 隐私政策

最后更新：2026-07-12

饭搭子是一个离线优先的家庭做饭助手。默认情况下，你的冰箱、计划、购物清单、做饭记录、饭团进度和本机家庭设置都保存在浏览器 localStorage 中。

## 数据存储

| 数据类型 | 默认位置 | 是否上传 |
|---|---|---|
| 冰箱食材 | 浏览器 localStorage | 仅在用户开启家庭同步时 |
| 菜品计划 | 浏览器 localStorage | 仅在用户开启家庭同步时 |
| 购物清单 | 浏览器 localStorage | 仅在用户开启家庭同步时 |
| 做饭记录 / 我家版 | 浏览器 localStorage | 仅在用户开启家庭同步时 |
| 饭团游戏数据 | 浏览器 localStorage | 仅在用户开启家庭同步时 |
| 健康事实 | 用户明确表达并确认后写入本地健康档案 | 默认不上传；使用家庭同步时按用户配置同步 |
| 饭团 AI 对话 | 用户主动发起并配置外部 AI 后发送必要上下文 | 发送到用户选择的 AI 服务 |
| Supabase 账号密码 | 不由饭搭子保存 | 由用户自己的 Supabase 项目管理 |

## 家庭同步

用户可自行配置 Supabase 作为家庭共享后端。一个家庭只需要一个共享 Supabase 后端；一个人也可以使用一个家庭组。

请只使用 Supabase anon key。不要把 service_role key、GitHub PAT 或任何私人 token 放进仓库。

## 外部服务

饭团 AI 支持用户自配 DeepSeek、OpenAI 或其他 OpenAI-compatible 服务。只有用户配置 Key 并主动发起 AI 请求时，当前对话所需的冰箱、计划、口味和已记录健康事实才会随请求发送到用户选择的服务。Key 保存在本机 localStorage；配置家庭同步时，AI 配置可写入用户自己的 Supabase 家庭设置。

未配置 Key 时，饭团使用本地 fallback，不会伪装成已调用外部模型。饭搭子不代用户注册或托管外部 AI 账号，也不把 Key 写入仓库、公开 Demo 或项目日志。

## 删除数据

- 本地数据：清除浏览器 localStorage。
- 云端数据：登录用户自己的 Supabase 控制台删除对应数据或项目。

## 追踪

饭搭子不使用 Cookie，不内置第三方分析或追踪工具。
